server {
    listen 80;
    server_name hw.edigistay.com www.hw.edigistay.com;

    # location / {
    #     # If you have a frontend to serve from this domain, configure it here.
    #     # For just the backend, you might not need this or could have a simple placeholder.
    #     # Example for serving static files (if you had a basic frontend):
    #     # root /var/www/hw.edigistay.com/html;
    #     # index index.html index.htm;
    #     proxy_pass http://192.168.1.182:5006/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # Optional: You might want to serve your backend on a specific path, like /api/
    location /api/ {
        proxy_pass http://192.168.1.182:5005/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Optional: Redirect HTTP to HTTPS (configure after setting up SSL)
    # if ($scheme = http) {
    #     return 301 https://$host$request_uri;
    # }
}